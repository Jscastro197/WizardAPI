version: '3.8'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - AWS_REGION=us-east-2
      - COGNITO_USER_POOL_ID=us-east-2_I8Z1uYYww
      - COGNITO_CLIENT_ID=7kggrvvb84e0qusakt1caflcdm